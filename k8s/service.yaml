apiVersion: v1
kind: Service
metadata:
  name: tech-challenge-svc
  namespace: oficina
spec:
  type: NodePort
  selector:
    app: tech-challenge-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
      protocol: TCP
